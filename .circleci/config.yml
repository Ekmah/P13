version: 1.0
jobs:
  build:
    resource_class: large
    steps:
      - setup_remote_docker:
          version: 20.10.14
      - run:
          name: Building
          command: docker-compose up -d
  tests:
    steps:
      - run:
          name: Tests
          command: python manage.py test
#  deploy:
#    steps:
#      - run:
#          name: Publishing
#          command: |
#            heroku